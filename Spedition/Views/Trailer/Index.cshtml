@model IEnumerable<Trailer>

@{
    ViewData["Title"] = "Trailers";
}

<div class="container p-3">
    <div class="row pt-6">
        <div class="col-6">
            <h2 class="text-primary">Trailers list</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Trailer" asp-action="Create" class="btn btn-primary">
                Add new trailer
            </a>
        </div>
    </div>
    <br /><br />
    @if (Model.Count() > 0)
    {
        <table class="table table-brodered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.trailer_number)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.trailer_manufacturer)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.trailer_model)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.trailer_plate)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                <tr>
                    <td width="15%">@obj.trailer_number</td>
                    <td width="25%">@obj.trailer_manufacturer</td>
                    <td width="25%">@obj.trailer_model</td>
                    <td width="15%">@obj.trailer_plate</td>
                    <td class="text-center">
                        <div class="w-75 vtn-group" role="group">
                            <a asp-controller="Trailer" asp-route-Id="@obj.id_trailer" asp-action="Edit" class="btn btn-success">
                                Edit
                            </a>
                            <a asp-controller="Trailer" asp-route-Id="@obj.id_trailer" asp-action="Delete" class="btn btn-danger">
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>There are no trailers in database</p>
    }
</div>